[project]
name = "nist-pqc-rag-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "google-genai",
    "langchain-core",
    "langgraph",
    "llama-parse",
    "nest_asyncio",
    "python-dotenv",
    "rank-bm25",
]

[tool.setuptools]
packages = ["rag"]
